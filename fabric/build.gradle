plugins {
    id 'java'
}

group = 'fr.en0ri4n'
version = '1.0.0'

repositories {
    mavenCentral()
}

def mcVersion = project.hasProperty("mcVersion") ? project.mcVersion : "1.20"

dependencies {
    implementation project(":core")
    implementation project(":fabric:fabric-${mcVersion}")
}